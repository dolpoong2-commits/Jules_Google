# 멀티 AI 채팅 설정 가이드 (v7 - 채팅창 개수 설정)

이 문서는 멀티 AI 채팅 스크립트의 설치부터, 핵심 기능인 **채팅창 개수 설정**, **좌표 설정**, **고급 대화 모드**의 사용법까지 모든 과정을 안내합니다.

---

## 1. 최초 설정 (가장 먼저 하세요!)

### 1.1. 프로그램 설치
-   **PowerToys (선택 사항)**: 화면 분할을 위해 Microsoft Store에서 설치합니다.
-   **AutoHotkey (필수)**: [공식 웹사이트](https://www.autohotkey.com/)에서 **v2.0 이상 버전**을 설치합니다.

### 1.2. 스크립트 생성 및 실행
1.  원하는 폴더에 `MultiAiChat.ahk` 파일을 만들고, 제가 제공한 최종 코드를 붙여넣습니다.
2.  `MultiAiChat.ahk` 스크립트를 더블클릭하여 실행합니다.

### 1.3. 사용할 채팅창 개수 설정 (중요!)
1.  컨트롤러 창 하단의 **[설정]** 버튼을 클릭합니다.
2.  '설정' 창이 나타나면, **"사용할 채팅창 개수"** 드롭다운 메뉴에서 실제로 사용할 개수 (1~8)를 선택합니다. (예: 4개의 AI만 사용한다면 `4`를 선택)
3.  **[저장]** 버튼을 누릅니다. 메인 컨트롤러의 체크박스가 선택한 개수만큼으로 변경됩니다.

### 1.4. 좌표 설정
1.  방금 설정한 개수만큼 AI 채팅창을 화면에 띄웁니다.
2.  컨트롤러의 **[좌표 설정]** 버튼을 클릭합니다.
3.  안내에 따라, **1번 채팅창의 입력창부터 설정한 개수만큼 순서대로 클릭**합니다.
4.  "설정 완료" 메시지가 뜨면 모든 기본 설정이 끝납니다. 설정 내용은 `settings.ini` 파일에 자동 저장됩니다.

---

## 2. 기능 사용법

### 2.1. 일반 전송 모드
-   메시지를 입력하고, 보낼 대상을 체크한 뒤 **[전송]** 버튼을 누릅니다. 설정된 개수 내의 채팅창에만 메시지가 전송됩니다.

### 2.2. 고급 대화 모드
-   **[고급 대화]** 버튼을 눌러 시나리오 설정 창을 엽니다.
-   **[추가]** 버튼을 눌러 대화 단계를 만듭니다. '보내는/받는 AI' 목록에는 **사용자가 설정한 채팅창 개수**만큼만 표시됩니다.
-   시나리오를 완성하고 '첫 질문'과 '반복 횟수'를 설정한 뒤 **[대화 시작]** 버튼을 누릅니다.
-   대화 중단은 `Esc` 키를 누르세요.

### 2.3. 기타 기능
-   **테마 선택**: 드롭다운 메뉴로 컨트롤러의 디자인을 변경합니다.
-   **모두 선택/해제**: 현재 보이는 체크박스 전체를 제어합니다.
-   **상태**: 좌표가 설정된 개수만큼 모두 지정되었는지 여부를 보여줍니다.