# Playwright 기반 AI 토론 오케스트레이터 사용 방법

이 문서는 Node.js와 Playwright를 사용하여 여러 AI 챗봇 간의 토론을 자동화하는 프로젝트의 설치 및 사용 방법을 안내합니다.

## 1. 사전 준비물

이 프로젝트를 실행하기 위해서는 **Node.js**가 반드시 설치되어 있어야 합니다. 안정적인 실행을 위해 **버전 18 이상**을 권장합니다.

- **Node.js 설치 확인 방법:**
  터미널(명령 프롬프트)을 열고 다음 명령어를 입력하세요.
  ```bash
  node -v
  ```
  `v18.0.0` 이상의 버전이 표시되면 됩니다. 만약 설치되어 있지 않거나 버전이 낮다면, [Node.js 공식 웹사이트](https://nodejs.org/)에서 LTS 버전을 다운로드하여 설치해 주세요.

## 2. 설치 과정

프로젝트를 처음 사용하는 경우, 다음 단계를 순서대로 진행하여 필요한 모든 구성 요소를 설치합니다.

1.  **프로젝트 디렉토리로 이동**:
    터미널에서 이 프로젝트 파일들이 있는 폴더로 이동합니다.

2.  **프로젝트 의존성 설치**:
    프로젝트에 필요한 Playwright 라이브러리를 설치합니다. 이 명령어는 `package.json` 파일을 읽어 필요한 모든 패키지를 `node_modules` 폴더에 자동으로 설치합니다.
    ```bash
    npm install
    ```

3.  **Playwright 브라우저 설치**:
    Playwright가 제어할 웹 브라우저(Chromium)를 설치합니다.
    ```bash
    npx playwright install chromium
    ```
    *참고: `npm install` 과정에서 이 작업이 자동으로 수행될 수도 있지만, 만약을 위해 한번 더 실행하여 브라우저가 확실히 설치되도록 합니다.*

이제 프로젝트 실행에 필요한 모든 소프트웨어 설치가 완료되었습니다.

## 3. 설정 방법

스크립트를 실행하기 전에, 두 개의 설정 파일을 필요에 맞게 수정할 수 있습니다.

### `services.json`
이 파일은 자동화할 AI 챗봇 서비스의 정보를 담고 있습니다. 새로운 서비스를 추가하거나 기존 서비스의 정보(특히 웹사이트 구조 변경 시)를 수정할 수 있습니다.

-   `name`: 서비스의 이름 (예: "ChatGPT")
-   `url`: 서비스의 접속 주소
-   `inputSelectors`: 텍스트를 입력할 입력창의 CSS 셀렉터 후보들. 스크립트가 위 순서대로 요소를 찾습니다.
-   `send`: 메시지 전송 방법. `"enter"`로 설정하면 엔터 키를 누르고, 다른 셀렉터(예: `{"selector": "button[data-testid='send-button']"}`)를 지정하면 해당 버튼을 클릭합니다.
-   `lastMsgSelectors`: AI의 마지막 답변을 가져오기 위한 CSS 셀렉터 후보들.

### `seed.md`
이 파일은 토론의 시작점이 되는 초기 프롬프트입니다.
-   `<컨텍스트>`: 토론의 배경이 되는 정보(회사, 목표, 제약 조건 등)를 자유롭게 기술합니다.
-   `<요청>`: AI들이 첫 번째 라운드에서 수행해야 할 구체적인 과제를 명시합니다.

## 4. 실행 방법

모든 설정이 완료되면 다음 단계에 따라 스크립트를 실행합니다.

1.  **스크립트 실행**:
    터미널에서 다음 명령어를 입력합니다.
    ```bash
    node debate_orchestrator.js
    ```

2.  **최초 1회 수동 로그인**:
    스크립트가 실행되면 설정된 AI 서비스의 수만큼 Chromium 브라우저 창이 자동으로 열립니다.
    -   각 브라우저 창을 확인하며 **로그인이 필요한 모든 서비스에 직접 로그인**합니다. (쿠키 허용 팝업이 있다면 처리합니다.)
    -   이 과정은 최초 한 번만 필요하며, 로그인 정보는 `.profiles` 폴더에 세션으로 저장되어 다음 실행부터는 자동으로 로그인됩니다.

3.  **자동 토론 시작**:
    모든 브라우저에서 로그인이 완료되면, 스크립트를 실행했던 **터미널 창으로 돌아와 `Enter` 키**를 누릅니다.

이제 스크립트가 자동으로 각 AI에게 프롬프트를 전송하고, 답변을 수집하며, 라운드를 거쳐 토론을 끝까지 진행합니다. 터미널에서 진행 상황을 확인할 수 있습니다.

## 5. 결과물

모든 토론 라운드가 성공적으로 완료되면, 프로젝트 폴더에 `final_business_plan.md`라는 마크다운 파일이 생성됩니다.

이 파일에는 다음과 같은 내용이 체계적으로 정리되어 있습니다.
-   각 서비스의 1차 제안 내용
-   각 서비스의 교차 검증 내용
-   모든 내용을 종합한 합의 초안
-   초안에 대한 최종 피드백

이 파일을 열어 자동 생성된 사업 계획서 및 관련 문서를 확인하고 활용할 수 있습니다.