# Playwright Debate Orchestrator - 사용 가이드 (v1.1)

이 문서는 멀티 시나리오 및 웹 UI를 지원하는 Playwright 기반 AI 토론 오케스트레이터의 설치 및 사용 방법을 안내합니다.

## 1. 주요 기능

-   **웹 UI 제공**: 로컬 웹 UI(`http://localhost:3000`)를 통해 시나리오 선택, 서비스(대화창) 관리, 새 시나리오 추가를 손쉽게 할 수 있습니다.
-   **멀티 시나리오 지원**: `seed.md` 파일에 여러 프로젝트 시나리오를 정의하고, UI에서 원하는 시나리오를 선택하여 토론 주제를 동적으로 변경할 수 있습니다.
-   **유연한 서비스 관리**: UI에서 최대 8개의 AI 서비스를 추가/삭제하고, 서비스별로 다른 브라우저(`chrome`, `chromium`)나 개별 프로필 경로를 지정할 수 있습니다.
-   **Dry Run 모드**: 실제 브라우저를 실행하기 전에, 생성될 프롬프트를 미리 터미널에서 확인하여 의도대로 동작하는지 검증할 수 있습니다.

## 2. 설치 및 설정

프로젝트를 처음 사용하는 경우, 다음 단계를 순서대로 진행하세요.

1.  **사전 준비물 (Node.js 설치)**:
    -   터미널에서 `node -v`를 실행하여 Node.js **버전 18 이상**이 설치되어 있는지 확인합니다.
    -   설치되어 있지 않다면, [Node.js 공식 웹사이트](https://nodejs.org/)에서 LTS 버전을 설치해 주세요.

2.  **프로젝트 의존성 설치**:
    -   터미널에서 프로젝트 폴더로 이동한 후, 다음 명령어를 실행하여 `express`와 `playwright` 등 모든 필수 패키지를 설치합니다.
        ```bash
        npm install
        ```

3.  **Playwright 브라우저 설치**:
    -   `playwright`가 제어할 웹 브라우저를 설치합니다.
        ```bash
        npm run install:pw
        ```
        (또는 `npx playwright install chromium`)

## 3. 실행 흐름

프로젝트는 **설정(UI) → 실행(터미널)**의 두 단계로 진행됩니다.

### 단계 1: UI 서버로 설정 관리

먼저, 로컬 웹 UI 서버를 실행하여 토론 설정을 관리합니다.

1.  **UI 서버 실행**:
    터미널에서 다음 명령어를 실행합니다.
    ```bash
    npm run ui
    ```

2.  **웹 브라우저 접속**:
    웹 브라우저를 열고 `http://localhost:3000` 주소로 접속합니다.

3.  **UI에서 설정하기**:
    -   **① 시나리오 선택**: 드롭다운 메뉴에서 이번에 실행할 토론 주제(시나리오)를 선택하고 `[선택 저장]` 버튼을 클릭합니다.
    -   **② 서비스(대화창) 관리**:
        -   `[행 추가]` 버튼으로 토론에 참여시킬 AI 서비스(최대 8개)를 추가합니다.
        -   **이름**과 **URL**은 필수 항목입니다.
        -   **채널**: `chrome` 또는 `chromium`을 선택할 수 있습니다. 비워두면 Playwright의 기본 브라우저를 사용합니다.
        -   **프로필 경로**: 특정 Chrome 사용자 프로필을 사용하고 싶을 때 절대 경로를 입력합니다. (예: `C:\\Users\\사용자\\AppData\\Local\\Google\\Chrome\\User Data\\Default`). 비워두면 프로젝트 내 `.profiles` 폴더에 자동으로 생성됩니다.
        -   설정이 끝나면 `[저장]` 버튼을 클릭합니다.
    -   **③ 새 시나리오 추가**: 필요하다면 새로운 시나리오의 `컨텍스트`와 `요청`을 입력하고 `[추가]` 버튼을 눌러 `seed.md`에 새 시나리오를 등록할 수 있습니다.

### 단계 2: 터미널에서 오케스트레이터 실행

웹 UI에서 모든 설정이 완료되면, 터미널로 돌아와 실제 토론 스크립트를 실행합니다.

1.  **Dry Run (선택 사항, 권장)**:
    -   실제 브라우저를 띄우기 전에, 어떤 프롬프트가 생성될지 미리 확인합니다.
        ```bash
        npm run dry
        ```
    -   터미널에 출력되는 R0, R1 프롬프트가 의도한 대로 구성되었는지 검토합니다.

2.  **실제 실행**:
    -   모든 것이 준비되었다면, 다음 명령어로 실제 토론을 시작합니다.
        ```bash
        npm start
        ```

3.  **수동 로그인 및 토론 시작**:
    -   스크립트가 실행되면 `services.json`에 설정된 수만큼 브라우저 창이 열립니다.
    -   각 서비스에 **수동으로 로그인**하고, 쿠키 허용 등 초기 설정을 마칩니다.
    -   모든 창이 준비되면, 스크립트를 실행했던 **터미널로 돌아와 `Enter` 키**를 누릅니다.

## 4. 결과물 확인

모든 토론 라운드가 완료되면, `seed.md`의 `<설정>`에 정의된 `output_file` (기본값: `final_business_plan.md`)이 프로젝트 루트 폴더에 생성됩니다. 이 파일을 열어 최종 결과물을 확인할 수 있습니다.